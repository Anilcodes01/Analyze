/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js config options go here if you have any.
  // For a basic setup, this can be empty.
  experimental: {}, // You can remove the old turbopack setting
};

export default nextConfig;